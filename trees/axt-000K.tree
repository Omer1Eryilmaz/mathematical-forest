\title{SFP domain}
\date{2023-09-07}
\taxon{definition}
\author{Thomas Streicher}

\def\Nat{\mathbb{N}}

\p{
  A dcpo #{(D, \sqsubseteq)} is called \em{SFP} iff there exists an ascending
  chain #{\{ h_n : D \to D \}_{n \in \mathbb{N}}} of continuous functions such
  that
  \ol{
    \li{every #{h_n} is a projection operator,}
    \li{#{\bigsqcup_{n \in \Nat} h_n = \mathsf{id}_D}, and}
    \li{#{h_n(D)} is finite for every #{n \in \Nat}.}
  }
}
